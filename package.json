{
  "name": "unified-ai-workspace",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "pnpm --filter @uaw/web dev",
    "build": "pnpm --filter @uaw/web build",
    "start": "pnpm --filter @uaw/web start",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r --if-present run test"
  }
}
